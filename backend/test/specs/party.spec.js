const Party = require("../../src/models/party");

describe('Testing partidos Table', () => {
    test('get partidos', async () => {
        const res = [];
        expect(Array.isArray(res)).toBe(true);
    });
});